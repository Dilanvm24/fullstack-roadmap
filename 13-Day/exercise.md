1-Crea una variable que al almacene un número en formato de cadena y luego lo convierta a número para sumarle 10.

R// let = digiteNumero = "35";
let numeroDigitado = Number(digiteNumero);
let resultado = numeroDigitado + 10;
console.log(resultado);

2-Convertir booleano a número.
Convierte un valor booleano a número usando conversión explícita.
R// let verdadero = true;
let falso = false;
console.log(Number(verdadero));
console.log(Number(falso)); 